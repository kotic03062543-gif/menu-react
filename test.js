// Object การประกาศตัวแปร Object ใน JavaScript
// method object ใน JavaScript

// array ใน JavaScript
// array method ใน JavaScript

// filter find findIndex map reduce sort;
// some every includes

// replace split join slice splice;

// object
const Object = {
  key: "value",
  key2: "value2",
};

let b = ["a", "b", "c"];

console.log(Object["key2"]);

function test(a) {
  return `${a}` + " is a string";
}

console.log(test("A"));
